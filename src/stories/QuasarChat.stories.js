import QuasarChat from "../components/QuasarChat.svelte";

export default {
  title: "QuasarChat",
  component: QuasarChat,
  argTypes: {
    // Define any props and their types here.
  },
};

export const Default = () => ({
  Component: QuasarChat,
  // Pass any props to the component here.
});
